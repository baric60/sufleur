import { withObservable } from '../../../ui/utils/with-observable.utils';
import { RouteComponent } from '../../components/route/route.component';

export const Route = withObservable(RouteComponent)(() => ({}));
