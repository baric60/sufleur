import { Create } from '../../components/create/create.component';
import { withObservable } from '../../../../utils/with-observable.utils';

export const CreateContainer = withObservable(Create)(() => {
	return {};
});
