import { Observable, OperatorFunction } from 'rxjs';
import { RemoteData } from '@devexperts/remote-data-ts';
export declare const switchMapRD: <L, A, B>(f: (a: A) => Observable<RemoteData<L, B>>) => OperatorFunction<RemoteData<L, A>, RemoteData<L, B>>;
