import { OperatorFunction } from 'rxjs';
import { RemoteData } from '@devexperts/remote-data-ts';
export declare const mapRD: <L, A, B>(f: (a: A) => B) => OperatorFunction<RemoteData<L, A>, RemoteData<L, B>>;
