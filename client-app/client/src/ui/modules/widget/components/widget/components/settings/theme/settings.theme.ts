const container = ``;

const content = ``;

const item = `
    margin-bottom: 30px;
`;

const control = `
    display: flex;
`;

const title = `
    font-weight: 600;
    font-size: 14px;
    line-height: 100%;
    color: #333333;
    opacity: 0.7;
    margin-bottom: 14px;
`;

const removeDescription = `
    font-size: 12px;
    line-height: 12px;
    color: #000000;
    opacity: 0.7;
`;

export const theme = {
	container,
	content,
	item,
	control,
	title,
	removeDescription,
};
